# Bina Pipeline — Python Dependencies
# Install: pip install -r requirements.txt

# Core ML
ultralytics>=8.3.0      # YOLO26 + training utilities  (bump if YOLO26 unsupported)
torch>=2.1.0
torchvision>=0.16.0

# Data processing
numpy>=1.24.0
opencv-python>=4.8.0
Pillow>=10.0.0
PyYAML>=6.0

# Utilities
tqdm>=4.66.0
roboflow>=1.1.0         # for downloading Roboflow datasets

# Hyperparameter optimization (Phase 2 — mini-runs, plan §5.1)
optuna>=3.5.0

# Phase 1B — non-Ultralytics specialist adapters
# torchvision already pinned above; covers Faster R-CNN ResNet-50-FPN.
transformers>=4.40.0    # HuggingFace DETR-R50 ("facebook/detr-resnet-50")
torchmetrics>=1.4.0     # mAP@0.5 eval for FRCNN+DETR; >=1.4 detects faster-coco-eval
faster-coco-eval>=1.6   # COCO eval backend torchmetrics MAP needs (better-maintained
                        # than pycocotools on Windows/Py3.13 — no C-build required)
pycocotools>=2.0.7      # fallback COCO backend that older torchmetrics also detect
timm>=0.9.0             # used by HF DETR backbone helpers
Pillow>=10.0.0          # already above; required by DetrImageProcessor

# Edge-export benchmarking (Phase 7 — plan §4.4 / §5.3)
onnx>=1.15.0
onnxruntime>=1.16.0     # use onnxruntime-gpu for CUDA benchmarking

# Optional: dataset download helpers
kaggle>=1.6.0           # needs ~/.kaggle/kaggle.json
requests>=2.31.0
